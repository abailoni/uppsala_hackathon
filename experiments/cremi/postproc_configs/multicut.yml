postproc_config:
  use_multicut: True
  extra_presets:
    - "simple_WSDT"
    - "multicut_exact"
  iterated_options:
    preset: ["multicut_exact"]
    edge_prob: 0.


#  multicut_kwargs:
#    weighting_scheme: "KeyDeleter"
